import { Options } from '@/media/demo/Options'

export const Bar = {
  barSize: Options.size,
  barDisplay: {
    list: [
      { value: undefined },
      { value: 'below' },
      { value: 'above' }
    ]
  }
}
