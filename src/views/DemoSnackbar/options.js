import {
  align,
  iconBackground,
  open,
  optionIcon,
  options,
  palette,
  shape,
  status,
  timeout
} from '@/media/demo/options'

export const optionsSnackbar = {
  status,
  open,

  options,
  palette,
  shape,
  align,
  timeout,

  optionIcon,
  iconBackground
}
